exports.ask = function () {
  console.log('pray');
}

/*
const randItems = {};
const randKeys = Object.keys(randSet);
console.log('randKeys', randKeys)
const itemKeys = [];
const randStart = parseInt((Math.random() * randKeys.length));
console.log('randStart', randStart)
for (let i = randStart; i <= randStart; i++) {
  const itemKey = randKeys[i];
  const title = randSet[itemKey].title;
  randItems[title] = randSet[itemKey];
  console.log(randItems[title])
}
*/